# Vendored ISPC grammar

Generated parser vendored from
<https://github.com/tree-sitter-grammars/tree-sitter-ispc>
at rev `ba1bb38ac8ddfa6aa7571cbfe9b4d029f7f77447` (v0.2.0), MIT licensed (see `LICENSE`).

Files: `parser.c` and `tree_sitter/` headers, as generated by tree-sitter-cli
(language ABI 15). Vendored because the upstream Rust bindings are broken at
this rev; `build.rs` compiles `parser.c` directly.

To update: copy `src/parser.c` and `src/tree_sitter/` from a newer rev and
bump the rev in this file.
