# Todos
 

collapsing nodes are broken - remove collapse feature entirely.

remove hub and spoke pattern from containers. figure out how to layout nodes inside containers. 





selected node should have all of its edges also selected. 



if something is called 400 times, the weight of each call should be lowered.
maybe weight the edge by its "importance" (1/degree)
are in-calls different than out calls ? 

may need to disable things caleld too many times

lower the opacity if its one of many 